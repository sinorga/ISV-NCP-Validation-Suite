#!/bin/bash
# AWS ISO/VMDK Import Tests Teardown Stub
# ISO import validation tests clean up their own resources during execution.
# This teardown is a no-op placeholder for consistency.

echo "No teardown required for ISO import tests." >&2
echo "ISO import validation tests clean up resources automatically." >&2
